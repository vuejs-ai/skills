Create:

1. `src/composables/useCart.ts` to manage cart items `{ id, name, price, quantity }`
2. `src/components/CartSummary.vue` that uses the composable

`CartSummary.vue` should render cart items, total price, and add/remove actions.
